# disk.conf — auto-generated by tools/get-device-spec.sh
#
# Source device : /dev/sdb
# Device model  : STORAGE DEVICE
# Device size   : 59638 MiB  (62534975488 bytes)
#
# All sizes are in mebibytes (MiB).
# IMPORTANT: values are MiB (1024-based), NOT MB (1000-based).

# disk_size_mib=59622  # 64 GB microdrive SD card
# disk_size_mib=64000  # 64 GB target device (restore for real etches)
# disk_size_mib=8192   # too small: full KDE6 + LibreOffice + dev-tools won't fit
# disk_size_mib=16384  # full extras set; needs a real >=16 GiB (17.2 GB) device
disk_size_mib=14800    # fits a "16 GB" USB stick (~14825 MiB usable); trimmed extras
efi_partition_size_mib=512
swap_size_mib=1024
